---
description: 前端按鈕與主要操作避免暗色系、維持可辨識度
globs: frontend/**/*.html
alwaysApply: false
---

# UI：避免難辨識的暗色按鈕

此專案庫存／入庫頁以淺色底為主。修改或新增**主要操作按鈕**（例如「新增分類」「確認」）時：

- **不要**僅使用 `bg-slate-700`、`bg-slate-800`、`bg-gray-800` 等與淺底對比不足或看起來髒暗的配色當主色。
- 優先使用**高對比**：亮黃／琥珀（例如 `#fde047`、amber）、藍（與既有「搜尋」一致）、或白底＋深色字＋清楚邊框。
- 重要按鈕請在頁內 `<style>` 用 `#buttonId { ... !important; }` 固定色，避免 Tailwind 暗色 class 覆蓋。

使用者要求「黃色」時：用**亮黃底＋深色字＋琥珀色邊框**，不要用深灰或暗黃褐當主色。
